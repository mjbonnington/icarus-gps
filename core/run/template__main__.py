#!/usr/bin/python

# [Icarus] Title source_filename.py
# v0.1
#
# Mike Bonnington <mike.bonnington@gps-ldn.com>
# (c) 2016 Gramercy Park Studios
#
# Description.


from PySide import QtCore, QtGui
from myApp_ui import * # <- import your app's UI file (as generated by pyside-uic)
import os, sys

# Import custom modules


class nameOfMyApp(QtGui.QMainWindow):

	def __init__(self, parent = None):
		super(nameOfMyApp, self).__init__()
		self.ui = Ui_MainWindow()
		self.ui.setupUi(self)


if __name__ == "__main__":
	app = QtGui.QApplication(sys.argv)

	# Initialise Icarus environment
	sys.path.append(os.environ['IC_WORKINGDIR'])
	import env__init__
	env__init__.setEnv()
	#env__init__.appendSysPaths()

	import rsc_rc # TODO: Check why this isn't working from within the UI file

	#app.setStyle('fusion') # Set UI style - you can also use a flag e.g. '-style plastique'

	# Apply UI style sheet
	qss=os.path.join(os.environ['IC_WORKINGDIR'], "style.qss")
	with open(qss, "r") as fh:
		app.setStyleSheet(fh.read())

	myApp = nameOfMyApp()
	myApp.show()
	sys.exit(app.exec_())

else:
	myApp = nameOfMyApp()
	print myApp
	myApp.show()

